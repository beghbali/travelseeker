branch-defaults:
  default:
    environment: production
  master:
    environment: production-clone
global:
  application_name: zentrips
  default_ec2_keyname: aws-eb
  default_platform: Ruby 2.1 (Passenger Standalone)
  default_region: us-west-1
  profile: eb-cli
